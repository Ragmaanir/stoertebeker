#!/bin/bash
#cd ./browser && npm start || echo "ERROR: Failed to start server"
ls browser/node_modules/.bin
cd ./browser && echo "TSC" && tsc && echo "ELECTRON" && node_modules/.bin/electron .built/index.js
